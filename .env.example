# Copy this file to .env and fill in your values
# .env is gitignored — never commit real keys

# LLM source: "local" | "anthropic" | "openai"
LLM_SOURCE=local

# Required only if LLM_SOURCE=anthropic
ANTHROPIC_API_KEY=

# Required only if LLM_SOURCE=openai
OPENAI_API_KEY=
